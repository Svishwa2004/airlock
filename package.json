{
  "name": "airlock",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "A WebMCP data workbench where an AI agent analyses data it never receives.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "node --test --experimental-strip-types src/data.test.ts"
  },
  "dependencies": {
    "papaparse": "5.5.3"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "@types/papaparse": "5.3.16",
    "chrome-remote-interface": "0.33.3",
    "typescript": "5.9.2",
    "vite": "8.2.2"
  }
}
